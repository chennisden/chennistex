\ProvidesPackage{notes} % this is to be used IN CONJUNCTION with dennis.sty, and MUST be loaded after

% mathcal and mathBB

\newcommand{\mbR}{\mathbb{R}} % real numbers
\newcommand{\mbZ}{\mathbb{Z}} % integers
\newcommand{\mbN}{\mathbb{N}} % natural numbers
\newcommand{\mbNp}{\mathbb{N}^{+}} % positive natural numbers
\newcommand{\mbNn}{\mathbb{N}^{0}} % non-negative natural numbers
\newcommand{\mbC}{\mathbb{C}} % complex numbers
\newcommand{\mbF}{\mathbb{F}} % fields

% Linear Algebra, Chabot College/LADR

%\newcommand{\spa}[1]{\text{Span}[#1]}
%bring back if mismath dies
\newcommand{\proj}[2]{\text{proj}_{\vec{#1}}(\vec{#2})}
\newcommand{\prp}[2]{\text{perp}_{\vec{#1}}(\vec{#2})}
\usepackage{mismath} % provides \adj \spa \im

% Abstract Algebra, Michael Artin

\newcommand{\nzr}{\mathbb{R}^{\times}} % non-zero reals - must be done in math mode
\newcommand{\gl}[2]{\text{GL}_{#1}(#2)} % general linear groups
\renewcommand{\im}{\text{im }\phi} % image, renew -> new if mismath dies
\newcommand{\imN}{\text{im }} % image text, naked
\newcommand{\ke}{\text{ker }\phi} % kernel. note that \ker is already defined for naked ker text.

% AoPS Calculus, David Patrick

% notation
\newcommand{\lm}[1]{\lim\limits_{#1}}

% envs because there's a LOT of problems, so enumerate works best

\newenvironment{exsol} % exercise solution
	{
	{{\color{sangria}\sffamily\textbf{Solution:}}}
	}
	{
	}
	
\newenvironment{rem} % remark
	{
	{{\color{darkmidnightblue}\sffamily\textbf{Remark:}}}
	}
	{
	}
	
\renewcommand{\proof}{
{\color{sangria}\sffamily\bfseries Proof:}
}